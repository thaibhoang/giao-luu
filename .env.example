# Sao chép thành `.env` ở gốc repo (docker compose đọc file này nếu có).
# Không commit `.env`.

# Bắt buộc nếu container Rails cần giải mã credentials (khớp nội dung web/config/master.key)
# RAILS_MASTER_KEY=
