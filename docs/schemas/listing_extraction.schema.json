{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sportmatch.local/schemas/listing_extraction.schema.json",
  "title": "SportMatchListingExtraction",
  "description": "Structured extraction from free-text recruiting posts (badminton / pickleball). Validates the \"extracted\" object in docs/API_CONTRACTS.md ingest body. Bump ingest schema_version when making incompatible changes (v2: skill_level slug mới).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "sport",
    "location_name",
    "start_time",
    "end_time",
    "slots_needed",
    "skill_level",
    "contact_info"
  ],
  "properties": {
    "sport": {
      "type": "string",
      "enum": ["badminton", "pickleball"]
    },
    "location_name": {
      "type": "string",
      "minLength": 1,
      "description": "Venue name or address snippet for display and geocoding"
    },
    "start_time": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601; prefer Z (UTC) or explicit offset"
    },
    "end_time": {
      "type": "string",
      "format": "date-time"
    },
    "slots_needed": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of players/slots still needed"
    },
    "skill_level": {
      "type": "string",
      "enum": [
        "yeu",
        "trung_binh_yeu",
        "trung_binh_minus",
        "trung_binh",
        "trung_binh_plus",
        "trung_binh_plus_plus",
        "trung_binh_kha",
        "kha",
        "ban_chuyen",
        "chuyen_nghiep"
      ],
      "description": "Slug trình độ chuẩn hóa; map cụm tiếng Việt trong SCRAPER_AGENT.md; nhãn UI — Listing::SKILL_LEVEL_LABELS"
    },
    "price_estimate": {
      "type": ["integer", "null"],
      "minimum": 0,
      "description": "Estimated fee per person in VND when mentioned; null if unknown"
    },
    "contact_info": {
      "type": "string",
      "minLength": 1,
      "description": "Prefer canonical Facebook post URL; no raw phone/email unless policy allows"
    }
  }
}
